README.txt

Much of the old algorithms for data processing, the data itself, and 
algorithms for future processing are stored in the files. For now, the files
of concern are stored only in the directory. The function to be parallelized
and then run on the HPCC is single_variable_classifier_all_features.m.